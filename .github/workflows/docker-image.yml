name: Docker Image CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: listing all directory
      run: |
          pwd
          ls
    - name: Login to DockerHub
      uses: docker/login-action@v1 
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: adminmindgrep/orchestrator:${{ GITHUB_SHA }}

    # - name: Build and push
    #   uses: docker/build-push-action@v2
    #   with:
    #     push: true
    #     context: .
    #     file: Dockerfile
    #     tags: adminmindgrep/orchestrator:$(date +%s)
    # - name: Build and Push Docker Image
    #   uses: mr-smithers-excellent/docker-build-push@v5
    #   with:
    #     image: adminmindgrep/orchestrator
    #     registry: docker.io
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Application Deployment
      run: |
          cd helm-chart
          helm template --dry-run .
          
#  helm install orchestrator . -n orchestrator --set image.repository=adminmindgrep/orchestrator